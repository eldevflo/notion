{
  "name": "notion",
  "version": "1.0.0",
  "description": "",
   "private": true,
   "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npm run start --workspace server",
    "client": "npm run dev --workspace client",
    "dev": "concurrently -c \"#ff8375,#95bf47\" -n client,server  \"npm run client\" \"npm run server\""
  },
  "author": "",
  "license": "ISC"
}
